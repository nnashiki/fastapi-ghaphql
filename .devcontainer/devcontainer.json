{
  "name": "fastapi-graphql",
  "dockerComposeFile": ["../docker-compose.yml", "docker-compose.yml"],
  "service": "api",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "/wait && poetry run alembic upgrade head;",
  "extensions": [
    "ms-python.python"
  ],
  "shutdownAction": "none"
}